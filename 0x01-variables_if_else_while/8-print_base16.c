#include <stdio.h>
/**
 *main - Entry point
 *Return: 0 (Success)
 */
int main(void)
{
int x;
int y;
for (x = '0'; x <= '9'; x++)
putchar(x);
for (y = 'A'; y <= 'E'; x++)
putchar(y);
putchar ('\n');
return (0);
}
