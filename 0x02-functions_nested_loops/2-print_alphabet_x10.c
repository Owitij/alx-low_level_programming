#include "main.h"
/**
 *main - entry point
 *print_alphabet_x10 - prints 10 times the alphabet, in lowercase.
 *Return: 0 (Success)
 */
void print_alphabet_x10(void)
{
char x;
int count;
while (count++ count < 9)
{
for (x = 'a'; x <= 'z'; x++)
_putchar(x)
_putchar('\n');
}
}

